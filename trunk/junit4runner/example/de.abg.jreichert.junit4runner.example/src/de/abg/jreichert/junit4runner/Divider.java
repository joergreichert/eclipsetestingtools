package de.abg.jreichert.junit4runner;

import java.math.BigDecimal;

public class Divider {
	
	public BigDecimal divide(BigDecimal divident, BigDecimal divisor) {
		return divident.divide(divisor);
	}
}
